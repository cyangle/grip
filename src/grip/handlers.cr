module Grip
  # `Grip::Handlers` is a module which contains the Exception, HTTP, Log, Pipeline and WebSocket handlers.
  # It contains the route class which provides the wrapper around an HTTP route.
  module Handlers; end
end
